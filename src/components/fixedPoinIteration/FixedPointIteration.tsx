import React from 'react';
import X0_input from "../fixedPoinIteration/X0_matrix";

const FixedPointIteration = () => {
    return (
        <div>
            <X0_input size={4}/>
        </div>
    );
};

export default FixedPointIteration;
